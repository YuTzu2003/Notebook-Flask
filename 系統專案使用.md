# PDF 做筆記：Windows 部署說明

## 正式架構

```text
使用者瀏覽器 → IIS（靜態快取）+ ARR（反向代理、分流）
                                     ↓
            Waitress 工作程序（127.0.0.1:50001、127.0.0.2:50002...）→ Flask
                                     ↓
                    SQL Server + 共用 tasks/ PDF 與結果檔案
```

- 對外只開放 `PUBLIC_HTTP_PORT`（正式 HTTPS 為 443）；不要公開 Waitress 連接埠。
- IIS 直接提供 `/static/` 的 CSS、JavaScript 與圖片，快取七天；其他請求由 ARR 分流至 Waitress。
- 每個 Waitress 工作程序只監聽 loopback 位址，由 ARR Server Farm 分流。
- 所有程序必須共用同一份 `.env` 與 `SECRET_KEY`，登入 session 才可跨程序使用。
- `NotebookFlask-01` 是唯一啟用每日備份排程的工作程序；其他程序會停用排程，避免重複備份。
- PDF 比對與筆記轉移會先寫入 SQL Server 佇列，再由獨立的 Queue Worker 依序處理。

## 部署前需求

1. Windows Server 或固定開機的 Windows 電腦。
2. Python 3.12、[uv](https://docs.astral.sh/uv/) 與 Git。
3. SQL Server 與 Microsoft ODBC Driver 18（17 亦可）。
4. IIS、IIS URL Rewrite 2.0 與 IIS Application Request Routing (ARR)。ARR 的 Server Proxy Settings 必須啟用 **Enable proxy**。
5. SQL Server 服務帳號對備份資料夾的寫入權限。

建議起點是 4 核心、16 GB RAM、SSD。大型 PDF 工作會競爭 CPU 與磁碟 I/O。

### 安裝 IIS、URL Rewrite 與 ARR

Windows Server 以系統管理員 PowerShell 安裝 IIS：

```powershell
Install-WindowsFeature Web-Server, Web-Mgmt-Tools
```

Windows 10／11 請在「開啟或關閉 Windows 功能」啟用 **Internet Information Services**、**Web Management Tools** 與 **World Wide Web Services**。接著從 [URL Rewrite 下載頁](https://www.iis.net/downloads/microsoft/url-rewrite) 安裝 URL Rewrite 2.0，再從 [ARR 下載頁](https://www.iis.net/downloads/microsoft/application-request-routing) 安裝 ARR。

執行 `inetmgr`，在伺服器節點開啟 **Application Request Routing Cache** → **Server Proxy Settings**，勾選 **Enable proxy**。完成後應可在 IIS Manager 看到 **URL Rewrite**、**Application Request Routing Cache** 與 **Server Farms**。

完整安裝、ARR Proxy 指令與一鍵部署順序請見 [架設步驟.md](架設步驟.md)。

## 第一次建置

```powershell
git clone https://github.com/YuTzu2003/Notebook-Flask.git C:\NotebookFlask
Set-Location C:\NotebookFlask
uv sync
Copy-Item .env.example .env
```

先在 SQL Server 建立 `Notebook` 資料庫與網站專用 SQL Login。第一次執行初始化時，該帳號需有建立資料表與索引權限；完成後可收回 DDL 權限，只保留日常讀寫權限。

`.env` 的正式環境起始設定：

```ini
APP_ENV=production
APP_DEBUG=false
SECRET_KEY=至少32字元的隨機值
DATABASE_URL=mssql+pyodbc://notebook_app:密碼@127.0.0.1:1433/Notebook?driver=ODBC+Driver+18+for+SQL+Server&TrustServerCertificate=yes

APP_WORKERS=1
PUBLIC_HTTP_PORT=80
BACKEND_BASE_PORT=50001
WAITRESS_HOST=127.0.0.1
WAITRESS_THREADS=8
PDF_TASK_CONCURRENCY=1

SESSION_COOKIE_SECURE=false
ENABLE_SCHEDULER=true
DB_BACKUP_DATABASE=Notebook
DB_BACKUP_PATH=D:/NotebookBackups/Notebook.bak
```

純 HTTP 內網請維持 `SESSION_COOKIE_SECURE=false`。在 IIS 設定 HTTPS binding 與憑證後，將它改為 `true`，並重新啟動所有 `NotebookFlask-*` 工作。

初始化或升級資料表：

```powershell
uv run python .\deploy\init_database.py
```

此命令可重複執行，不會刪資料。

## IIS + ARR 設定

安裝 IIS、URL Rewrite 與 ARR 後，在 IIS Manager 的伺服器節點開啟 **Application Request Routing Cache** → **Server Proxy Settings** → 勾選 **Enable proxy**。

以系統管理員 PowerShell 部署：

```powershell
Set-Location C:\NotebookFlask
.\deploy\deploy-production.ps1
```

若前置條件已完成，可改用 `.\deploy\setup-and-deploy.ps1` 先驗證 IIS + ARR，再一鍵部署。

部署腳本會建立 IIS 站台 `NotebookFlask`、ARR Server Farm `NotebookFlaskBackend`、`/static/` 虛擬目錄及其七天快取。ARR 會依 `APP_WORKERS` 建立後端節點；每個節點使用不同 loopback 位址與 `BACKEND_BASE_PORT + 工作程序索引`。

若 80 已由另一個 IIS 站台占用，請設定未占用的 `PUBLIC_HTTP_PORT` 後重新部署。

### 同一台主機的其他 IIS 系統

目前部署腳本以不同公開連接埠支援同機多系統。每個系統都必須使用不同的 `PUBLIC_HTTP_PORT` 與 `BACKEND_BASE_PORT` 範圍，例如：

| 系統 | `PUBLIC_HTTP_PORT` | `BACKEND_BASE_PORT` | 開啟網址 |
| --- | --- | --- | --- |
| Notebook Flask | `5000` | `50001` | `http://伺服器IP:5000/` |
| 另一個系統 | `5001` | `51001` | `http://伺服器IP:5001/` |

Notebook Flask 部署時可指定不同 IIS 站台名稱：

```powershell
.\deploy\deploy-production.ps1 -IisSiteName NotebookFlask
```

若兩個系統要共用 80／443 並分別用不同網域名稱，需為每個 IIS 站台設定 Host Name binding 與 HTTPS 憑證；目前這個部署腳本不會自動建立 Host Name binding。

## 啟動與自動開機

先手動驗證第一個工作程序：

```powershell
Set-Location C:\NotebookFlask
.\deploy\start-production.ps1 -Port 50001 -RunScheduler
Invoke-RestMethod http://127.0.0.1:50001/health
```

預期結果包含 `"environment":"production"`。測試後以 `Ctrl+C` 停止。

一鍵部署會註冊並啟動自動開機工作。若只要重新註冊：

```powershell
Set-Location C:\NotebookFlask
.\deploy\register-autostart.ps1
Get-ScheduledTask -TaskName "NotebookFlask-??" | ForEach-Object {
    Start-ScheduledTask -TaskName $_.TaskName
}
Start-ScheduledTask -TaskName NotebookFlaskTaskWorker
```

工作程序與佇列日誌位置：

```text
tasks\logs\notebook-flask-50001.log
tasks\logs\notebook-flask-task-worker.log
```

移除自動開機：

```powershell
.\deploy\unregister-autostart.ps1
```

## Debug 與正式模式

開發模式：

```ini
APP_ENV=development
APP_DEBUG=true
```

```powershell
uv run python app.py
```

正式模式：

```ini
APP_ENV=production
APP_DEBUG=false
```

切換後需重新啟動所有 `NotebookFlask-*` 工作。變更 `APP_WORKERS`、`PUBLIC_HTTP_PORT` 或 `BACKEND_BASE_PORT` 後，重新執行 `deploy-production.ps1`，使 IIS + ARR 與開機工作同步更新。

## 併發與效能調校

| 情境 | 建議值 | 說明 |
| --- | --- | --- |
| 2 核心 / 16 GB | `APP_WORKERS=1`、`WAITRESS_THREADS=8`、`PDF_TASK_CONCURRENCY=1` | 一般操作可多人使用，PDF 工作依序排隊。 |
| 4 核心 / 16 GB | `APP_WORKERS=2`、`WAITRESS_THREADS=8`、`PDF_TASK_CONCURRENCY=1` | 可再評估增加第二個 Web 程序；Queue Worker 仍維持一個。 |
| 8 核心 / 32 GB | `APP_WORKERS=3`、`WAITRESS_THREADS=8`、`PDF_TASK_CONCURRENCY=1` | 改完要重建 IIS + ARR 與開機工作。 |
| PDF 工作造成卡頓 | 先降低 `PDF_TASK_CONCURRENCY` | 優先保留登入、查詢與一般頁面回應。 |
| 靜態資源載入慢 | 確認 IIS 已建立 `/static/` 虛擬目錄 | CSS/JS 不應每次交由 Flask 提供。 |

不要只提高 thread 或 worker 數。每個程序都有 SQL 連線池，PDF 工作也會競爭 CPU、記憶體與磁碟。

## 上版與維運順序

1. 備份 SQL Server 與整個 `tasks/` 目錄。
2. 停止 `NotebookFlask-*` 排程工作。
3. 更新程式碼並執行 `uv sync`。
4. 執行 `uv run python .\deploy\init_database.py`。
5. 檢查 `.env`；若改動 `APP_WORKERS`、`PUBLIC_HTTP_PORT` 或 `BACKEND_BASE_PORT`，重新執行 `deploy-production.ps1`。
6. 啟動所有工作程序，檢查 IIS + ARR `/health`、登入、上傳、比對與筆記轉移。
7. 確認 `tasks\logs` 無例外後再開放使用者。

SQL Server 備份不包含 `tasks/` 裡的 PDF 與比對結果，兩者必須一起備份。
