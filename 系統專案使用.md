# 筆記系統專案使用

## 系統架構

正式環境的請求流程為：使用者瀏覽器 → Nginx（80/443）→ Waitress（僅監聽 `127.0.0.1:50001`）→ Flask → SQLAlchemy 連線池 → SQL Server。Nginx 提供反向代理與上傳限制；Waitress 執行緒和 SQLAlchemy 連線池負責多人同時操作。

目前背景 PDF 工作與檔案都存放在本機 `tasks/`，因此正式環境應只啟動一個 Waitress 程序。不要直接啟動多個 Flask/Waitress 程序；若未來要多主機或多程序，需先把檔案改成共享儲存，並把背景工作移至 Redis/Celery 等工作佇列。

## 安裝

需求：Python 3.12 以上、uv、SQL Server、Microsoft ODBC Driver 17 或 18、Nginx。

```powershell
cd D:\YLH\notebook_flask
uv sync
Copy-Item .env.example .env
```

現有 `.env` 已保留舊資料庫參數並新增環境切換與 SQLAlchemy URL。不要把 `.env` 提交到 Git。

## `.env` 設定

開發模式：

```ini
APP_ENV=development
APP_DEBUG=true
APP_HOST=127.0.0.1
APP_PORT=50001
ENABLE_SCHEDULER=false
```

正式模式：

```ini
APP_ENV=production
APP_DEBUG=false
APP_HOST=127.0.0.1
APP_PORT=50001
WAITRESS_THREADS=16
TRUSTED_PROXY=127.0.0.1
PROXY_COUNT=1
ENABLE_SCHEDULER=true
```

產生正式密鑰並填入 `SECRET_KEY`：

```powershell
uv run python -c "import secrets; print(secrets.token_hex(32))"
```

SQLAlchemy 使用單一字串：

```ini
DATABASE_URL=mssql+pyodbc://user:password@127.0.0.1:1433/Hospital?driver=ODBC+Driver+17+for+SQL+Server&TrustServerCertificate=yes
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=1800
```

帳號或密碼含 `@`、`:`、`/`、`#` 等字元時必須先做 URL encoding。正式環境若已由有效憑證保護 SQL Server，應移除 `TrustServerCertificate=yes` 並使用憑證驗證。

`DB_BACKUP_PATH` 是 SQL Server 服務所在主機看到的路徑，不一定是 Flask 主機的路徑。SQL Server 服務帳號必須有該目錄寫入權限。

## 啟動與切換模式

開發模式直接執行：

```powershell
uv run python app.py
```

正式模式先把 `.env` 的 `APP_ENV` 改成 `production`、`APP_DEBUG` 改成 `false`，並換掉 `SECRET_KEY`，再執行：

```powershell
powershell -ExecutionPolicy Bypass -File .\deploy\start-production.ps1
```

健康檢查：

```powershell
Invoke-RestMethod http://127.0.0.1:50001/health
```

正常結果為 `status=ok`、`environment=production`。

## Nginx

把 `deploy/nginx/notebook_flask.conf` 放入 Nginx 的 `conf.d`，或在 `nginx.conf` 的 `http {}` 中加入：

```nginx
include D:/YLH/notebook_flask/deploy/nginx/notebook_flask.conf;
```

檢查並重新載入：

```powershell
nginx -t
nginx -s reload
```

對外只開放 Nginx 的 80/443；不要把 50001 對外開放。正式對外服務應在 Nginx 設定 TLS 憑證，並將 `.env` 的 `SESSION_COOKIE_SECURE` 改為 `true`。

Windows 長期執行可使用 NSSM 或工作排程器，把工作目錄設為 `D:\YLH\notebook_flask`，啟動命令設為 `powershell.exe -ExecutionPolicy Bypass -File D:\YLH\notebook_flask\deploy\start-production.ps1`。Flask/Waitress 與 Nginx 都應設定開機啟動與失敗後重新啟動。

## 多人資料隔離

- 個人 PDF、編輯內容、筆記轉移下載與轉移狀態都以登入者 `UserID` 查詢。
- 文件版本與已發布的版本比對資料屬於系統共用資料。
- Waitress 預設 16 個請求執行緒；SQLAlchemy 預設 10 個常駐連線、最多再增加 20 個。
- PDF 比對與筆記轉移各限制最多 3 個背景工作，避免大型 PDF 同時耗盡記憶體。

## 筆記轉移

1. 在「文件版本」上傳舊版與新版 PDF。
2. 在「版本比對」建立頁面映射，等待狀態成為成功。
3. 在「筆記轉移」選擇映射並上傳帶註記的舊版 PDF。
4. 等待工作完成後下載 `Move_` 開頭的結果 PDF。

新版比對使用中文字元 n-gram TF-IDF 和單調動態規劃。跨頁大方框會先依段落切分，再以相鄰頁映射限定搜尋範圍，避免跳到下一章相似文字。對低可信度映射仍應抽查轉移結果，尤其是新版已刪除的原文。

## 驗證與維護

快速測試：

```powershell
uv run python -m unittest test.test_config test.test_mapping_alignment
```

指定手冊的跨頁註記回歸測試約需數十秒：

```powershell
uv run python -m unittest test.test_annotation_migration_regression
```

資料庫連線檢查：

```powershell
uv run python -c "from modules.db import execute_query; print(execute_query('SELECT 1 AS value'))"
```

部署前至少確認：正式密鑰已更換、`APP_DEBUG=false`、50001 未對外開放、Nginx TLS 正常、SQL Server 備份可還原、兩種測試均通過。

## 參考來源

- Flask 官方 production、Nginx 與 ProxyFix 部署文件：https://flask.palletsprojects.com/en/stable/deploying/
- Waitress 參數文件：https://docs.pylonsproject.org/projects/waitress/en/latest/arguments.html
- SQLAlchemy SQL Server / pyodbc 文件：https://docs.sqlalchemy.org/en/20/dialects/mssql.html
- Nginx 官方反向代理文件：https://docs.nginx.com/nginx/admin-guide/web-server/reverse-proxy/
- PyMuPDF 官方 GitHub 與註記 API：https://github.com/pymupdf/PyMuPDF
- PyMuPDF 註記複製討論（應依類型用標準 API 重建外觀）：https://github.com/pymupdf/PyMuPDF/discussions/3577
- 可參考的完整部署架構：https://github.com/tiangolo/full-stack
