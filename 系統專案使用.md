# 系統專案使用手冊

## 部署前準備

| 項目 | 說明 |
| --- | --- |
| SQL Server | 請手動建立 Hospital 資料庫與應用程式 SQL Login。 |
| 應用程式帳號 | 首次建表時需有 dbo schema 的建表與建索引權限。完成後可收回 DDL 權限。 |
| Python | Python 3.12 與 uv。 |
| ODBC | 安裝 Microsoft ODBC Driver 17 或 18 for SQL Server。 |

## .env

.env 是唯一的應用程式資料庫連線來源。不要把帳密寫進 deploy.sh。

~~~ini
APP_ENV=production
APP_DEBUG=false
SECRET_KEY=請使用至少32字元的隨機值 (可使用 uv run python -c "import secrets; print(secrets.token_hex(32))" 產生)

DATABASE_URL=mssql+pyodbc://notebook_app:your-password@127.0.0.1:1433/Hospital?driver=ODBC+Driver+17+for+SQL+Server&TrustServerCertificate=yes

ENABLE_SCHEDULER=true
DB_BACKUP_DATABASE=Hospital
DB_BACKUP_PATH=C:/backups/Hospital_.bak
~~~

若密碼含有 @、:、/、?、# 或 %，請先做 URL encoding。

## 初始化資料表

執行下列命令會建立系統必要的資料表與索引：

~~~powershell
uv sync
uv run python .\deploy\init_database.py
~~~

(若是 Linux 環境，可直接執行 `bash deploy.sh` 來進行安裝與初始化)

這個命令可以重複執行，只會建立不存在的物件，不會刪除資料，也不會建立 SQL Server、資料庫或 SQL Login。

建立的資料表：

- Users：網站使用者。
- Documents：使用者上傳與編輯的 PDF。
- DocVersion：版本管理 PDF。
- MappingRecord：新舊版本頁碼比對紀錄。
- NoteTransferHistory：筆記轉移紀錄。
- audit_logs：系統紀錄。

SQL Login 與網站登入帳號不同。初始化後仍要在 Users 表新增第一位網站管理員，才能登入系統。

## 啟動與反向代理

正式環境：

~~~powershell
powershell -ExecutionPolicy Bypass -File .\deploy\start-production.ps1
Invoke-RestMethod http://127.0.0.1:50001/health
~~~

Nginx 對外提供 80/443，反向代理至 127.0.0.1:50001。不要直接公開 50001。

## 開機自動啟動

以系統管理員 PowerShell 執行：

~~~powershell
.\deploy\register-autostart.ps1
~~~

排程工作名稱是 NotebookFlask。它會以 SYSTEM 身分在開機後等待 30 秒啟動，並在失敗時重試三次。日誌位置：

~~~text
tasks/logs/notebook-flask.log
~~~

移除自動啟動：

~~~powershell
.\deploy\unregister-autostart.ps1
~~~

## 備份與還原

備份不能省略。建表腳本只能產生空表，無法還原任何既有使用者、PDF、版本比對或筆記轉移資料。

| 功能 | 執行方式 |
| --- | --- |
| 自動備份 | APP_ENV=production 與 ENABLE_SCHEDULER=true 時，應用程式每天 02:00 執行。 |
| 備份位置 | 由 DB_BACKUP_PATH 指定，且 SQL Server 服務帳號必須能寫入。 |
| 還原 | 停止系統後，以 SSMS 從 .bak 還原 Hospital。 |

目前備份使用 WITH INIT，會覆寫同一路徑的舊備份。正式環境建議另以 SQL Server Agent、Windows 排程或備份軟體保留多日副本。

## 資料生命週期：何時建表、備份與還原

三個動作用途不同，不能互相取代：

| 情境 | 要做的事 | 不要做的事 |
| --- | --- | --- |
| 第一次部署 | 手動建立 Hospital 與 SQL Login，設定 DATABASE_URL，再執行建表腳本。 | 不需要還原備份。 |
| 每日正常使用 | 保持系統運行，讓每日備份排程產生 .bak。 | 不需要再次執行建表或還原。 |
| 搬到新電腦 | 先安裝 SQL Server，再還原最近的 .bak。 | 還原前不要先以空資料庫覆蓋來源資料。 |
| 誤刪、資料損毀或主機故障 | 停止系統，從確認可用的 .bak 還原。 | 不要先執行建表腳本期待資料回來。 |

### 第一次部署的完整順序

1. 在 SQL Server 建立 Hospital 資料庫。
2. 建立給 Flask 使用的 SQL Login，並將該 Login 對應到 Hospital 的 dbo 使用者。
3. 暫時授予該使用者建立資料表與索引的權限。
4. 設定 .env 的 DATABASE_URL。
5. 執行建表：

~~~powershell
uv run python .\deploy\init_database.py
~~~

6. 到資料庫確認六張資料表已存在。
7. 手動新增第一位網站管理員到 Users 表。
8. 確認系統能登入後，可收回應用程式帳號的 DDL 權限，只保留日常讀寫權限。

### 日常自動備份的實際條件

下列三個條件必須同時成立，備份才會在每天 02:00 執行：

1. .env 設定 APP_ENV=production。
2. .env 設定 ENABLE_SCHEDULER=true。
3. Flask 與 Waitress 程序在 02:00 當下仍處於運行狀態。

手動測試備份可執行：

~~~powershell
uv run python -c "from modules.backup import run_database_backup; run_database_backup()"
~~~

成功後，請到 DB_BACKUP_PATH 指定的位置確認 .bak 檔案的修改時間已更新。

重要：BACKUP DATABASE 指令是由 SQL Server 執行。因此若 Flask 與 SQL Server 位於不同電腦，DB_BACKUP_PATH 是 SQL Server 那台電腦的路徑，不是 Flask 主機的路徑；SQL Server 服務帳號也必須對該資料夾具有寫入權限。

### 還原資料庫的完整順序

1. 先停止 Notebook Flask 排程工作或 Waitress，避免還原過程仍有使用者寫入資料。
2. 在 SQL Server Management Studio 連到目標 SQL Server。
3. 對 Databases 按右鍵，選擇 Restore Database。
4. 選擇 Device，加入要還原的 .bak 檔。
5. 確認目標資料庫是 Hospital；若要覆蓋現有資料庫，必須明確確認備份日期與覆蓋風險。
6. 執行還原後，確認 Users、Documents、DocVersion、MappingRecord、NoteTransferHistory、audit_logs 都存在。
7. 如還原的是較舊備份，可再執行建表腳本補上缺少的表或索引；它不會刪除還原資料。
8. 啟動系統並確認：

~~~powershell
Invoke-RestMethod http://127.0.0.1:50001/health
~~~

9. 使用一個既有帳號登入，確認文件、版本比對與筆記轉移紀錄可讀取。

### 備份保存建議

目前程式只維護一份固定名稱的備份檔。建議至少保留每日七份與每月一份：

- 每日備份：保留最近七天。
- 每月備份：保留最近十二個月。
- 至少複製一份到不同磁碟或其他主機，避免 SQL Server 主機或磁碟故障時備份一起遺失。
- 定期在非正式環境做一次還原演練；只有成功還原過的備份才可信任。
