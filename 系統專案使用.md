# PDF 做筆記系統：Windows 正式部署手冊

## 正式架構

```text
使用者瀏覽器 → Nginx（反向代理、靜態快取）
                      ↓ 127.0.0.1:BACKEND_BASE_PORT...
                 Waitress 工作程序（Flask）
                      ↓
          SQL Server + 共用 tasks/ PDF 與結果檔案
```

- 對外只開放 `PUBLIC_HTTP_PORT`（正式 HTTPS 則為 443）；不要公開 Waitress 連接埠。
- 每個 Waitress 程序只聽 `127.0.0.1`，由 Nginx 分流。
- 所有程序必須共用同一份 `.env` 與 `SECRET_KEY`，登入 session 才可跨程序使用。
- `NotebookFlask-01` 是唯一啟用每日備份排程的工作程序；其他程序會停用排程，避免重複備份。
- PDF 比對與筆記轉移會先寫入 SQL Server 佇列，再由獨立的 Queue Worker 依序處理；演算法未改。

Windows Nginx 適合作為既有反向代理與靜態檔伺服器，但不是效能擴充核心。若未來要公開網際網路或承受大量並發，建議改部署到 Linux；在目前 Windows 系統，這個多 Waitress 程序架構較容易快速維運。

## 部署前需求

1. Windows Server 或固定開機的 Windows 電腦。
2. Python 3.12、[uv](https://docs.astral.sh/uv/) 與 Git。
3. SQL Server 與 Microsoft ODBC Driver 18（17 亦可）。
4. Nginx for Windows；若使用 HTTPS，需另外準備憑證與 443 設定。
5. SQL Server 服務帳號對備份資料夾的寫入權限。

建議起點是 4 核心、16 GB RAM、SSD。大型 PDF 工作會競爭 CPU 與磁碟 I/O。

## 第一次建置

### 1. 安裝程式與依賴

```powershell
git clone https://github.com/YuTzu2003/Notebook-Flask.git C:\NotebookFlask
Set-Location C:\NotebookFlask
uv sync
Copy-Item .env.example .env
```

### 2. 建立資料庫與設定 `.env`

先在 SQL Server 建立 `Notebook` 資料庫與網站專用 SQL Login。第一次執行初始化時，該帳號需有建立資料表與索引權限；完成後可收回 DDL 權限，只保留日常讀寫權限。

`.env` 的正式環境起始設定：

```ini
APP_ENV=production
APP_DEBUG=false
SECRET_KEY=至少32字元的隨機值
DATABASE_URL=mssql+pyodbc://notebook_app:密碼@127.0.0.1:1433/Notebook?driver=ODBC+Driver+18+for+SQL+Server&TrustServerCertificate=yes

APP_WORKERS=1
PUBLIC_HTTP_PORT=80
BACKEND_BASE_PORT=50001
WAITRESS_HOST=127.0.0.1
WAITRESS_THREADS=8
PDF_TASK_CONCURRENCY=1

SESSION_COOKIE_SECURE=false
ENABLE_SCHEDULER=true
DB_BACKUP_DATABASE=Notebook
DB_BACKUP_PATH=D:/NotebookBackups/Notebook.bak
```

產生 `SECRET_KEY`：

```powershell
uv run python -c "import secrets; print(secrets.token_hex(32))"
```

只有 Nginx 已設定 HTTPS 時，才將 `SESSION_COOKIE_SECURE=true`。純 HTTP 內網若設為 `true`，瀏覽器不會送出登入 cookie。

### 3. 初始化或升級資料表

```powershell
uv run python .\deploy\init_database.py
```

此命令可重複執行，不會刪資料。它會建立缺少的表與索引，並將舊版 `audit_logs` 補成目前的 `Audit_logs` 欄位：`Action`、`User_id`、`Detail_json`、`Remote_addr`。

初始化後建立第一個網站管理員，並確認密碼已使用雜湊格式保存。

## Nginx 設定

依 `.env` 的 `APP_WORKERS` 產生 upstream 與靜態檔快取設定：

```powershell
.\deploy\configure-nginx.ps1 -ConfigPath C:\nginx\conf\notebook_flask.conf
```

在 `C:\nginx\conf\nginx.conf` 的 `http { ... }` 中加入：

```nginx
include notebook_flask.conf;
```

檢查並重載 Nginx：

```powershell
Set-Location C:\nginx
.\nginx.exe -t
.\nginx.exe -s reload
```

`/static/` 的 CSS、JavaScript、圖片會由 Nginx 快取七天；登入、API 與 PDF 下載不會快取。

## 啟動與自動開機

先手動驗證第一個工作程序：

```powershell
Set-Location C:\NotebookFlask
.\deploy\start-production.ps1 -Port 50001 -RunScheduler
Invoke-RestMethod http://127.0.0.1:50001/health
```

預期結果包含 `"environment":"production"`。測試後以 `Ctrl+C` 停止。

以系統管理員 PowerShell 註冊自動開機：

```powershell
Set-Location C:\NotebookFlask
.\deploy\register-autostart.ps1
Start-ScheduledTask -TaskName NotebookFlask-01
Start-ScheduledTask -TaskName NotebookFlaskTaskWorker
```

2 Core / 16 GB 的建議值是 `APP_WORKERS=1`，會建立 `NotebookFlask-01` 與 `NotebookFlaskTaskWorker`。工作程序與佇列日誌位置：

```text
tasks\logs\notebook-flask-50001.log
tasks\logs\notebook-flask-task-worker.log
```

移除自動開機：

```powershell
.\deploy\unregister-autostart.ps1
```

## Debug 與正式模式

開發模式：

```ini
APP_ENV=development
APP_DEBUG=true
```

```powershell
uv run python app.py
```

正式模式：

```ini
APP_ENV=production
APP_DEBUG=false
```

切換後需重新啟動所有 `NotebookFlask-*` 工作。變更 `APP_WORKERS`、`PUBLIC_HTTP_PORT` 或 `BACKEND_BASE_PORT` 後，還要重新執行 `configure-nginx.ps1` 與 `register-autostart.ps1`。

## 併發與效能調校

| 情境 | 建議值 | 說明 |
| --- | --- | --- |
| 2 核心 / 16 GB | `APP_WORKERS=1`、`WAITRESS_THREADS=8`、`PDF_TASK_CONCURRENCY=1` | 一般操作可多人使用，PDF 工作依序排隊。 |
| 4 核心 / 16 GB | `APP_WORKERS=2`、`WAITRESS_THREADS=8`、`PDF_TASK_CONCURRENCY=1` | 可再評估增加第二個 Web 程序；Queue Worker 仍維持一個。 |
| 8 核心 / 32 GB | `APP_WORKERS=3`、`WAITRESS_THREADS=8`、`PDF_TASK_CONCURRENCY=1` | 改完要重建 Nginx 與開機任務。 |
| PDF 工作造成卡頓 | 先降低 `PDF_TASK_CONCURRENCY` | 優先保留登入、查詢與一般頁面回應。 |
| 靜態資源載入慢 | 確認 Nginx 已提供 `/static/` | CSS/JS 不應每次交由 Flask 提供。 |

不要只提高 thread 或 worker 數。每個程序都有 SQL 連線池，PDF 工作也會競爭 CPU、記憶體與磁碟。

## 上版與維運順序

1. 備份 SQL Server 與整個 `tasks/` 目錄。
2. 停止 `NotebookFlask-*` 排程工作。
3. 更新程式碼並執行 `uv sync`。
4. 執行 `uv run python .\deploy\init_database.py`。
5. 檢查 `.env`；若改動 `APP_WORKERS`，重新產生 Nginx 設定並註冊工作。
6. 重載 Nginx，啟動所有工作程序，檢查 `/health`、登入、上傳、比對與筆記轉移。
7. 確認 `tasks\logs` 無例外後再開放使用者。

SQL Server 備份不包含 `tasks/` 裡的 PDF 與比對結果，兩者必須一起備份。
