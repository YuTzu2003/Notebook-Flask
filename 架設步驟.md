# Notebook Flask Windows 快速部署

本文件只保留正式環境必要步驟。部署腳本會讀取 `deploy\database\script.sql`，依 `.env` 的資料庫連線建立／升級資料表；並自動安裝專案套件、產生 Nginx 設定、開放網站連接埠、註冊開機自動啟動，等待後端健康檢查成功後才顯示完成。

## 1. 手動前置安裝

請先在 Windows 主機手動安裝以下工具。一般 x64 Windows 請選擇 x64 安裝檔。

| 工具 | 用途 | 官方連結 |
| --- | --- | --- |
| Git | 下載與更新專案 | [Git for Windows](https://git-scm.com/download/win) |
| uv | 安裝 Python 套件與建立虛擬環境 | [uv 安裝說明](https://docs.astral.sh/uv/getting-started/installation/) |
| Python 3.12 | 執行系統；也可交由 uv 管理 | [Python for Windows](https://www.python.org/downloads/windows/) |
| ODBC Driver 17 或 18 | Python 連線 SQL Server | [Microsoft ODBC Driver](https://learn.microsoft.com/sql/connect/odbc/download-odbc-driver-for-sql-server) |
| Nginx for Windows | 對外網站服務 | [Nginx 下載頁](https://nginx.org/en/download.html) |

SQL Server 可使用院內既有服務。請由 DBA 或具備 SQL Server 權限的人員建立：

- 資料庫：`Notebook`
- SQL Login：依院內命名規範建立，例如 `notebook_user`
- 首次部署帳號需有建立／更新資料表與索引的權限。

## 2. 下載專案與設定 `.env`

在一般 PowerShell 執行：

```powershell
git clone https://github.com/YuTzu2003/Notebook-Flask.git D:\Notebook-Flask
Set-Location D:\Notebook-Flask
Copy-Item .env.example .env
```

編輯 `D:\Notebook-Flask\.env`。資料庫位址、連接埠、帳密與網站連接埠都只在這個檔案設定；部署腳本不會覆寫它們。

```ini
APP_ENV=production
APP_DEBUG=false
SECRET_KEY=請填入至少32字元的隨機字串

# Driver 名稱必須和實際安裝的 ODBC Driver 版本相同。
DATABASE_URL=mssql+pyodbc://notebook_user:密碼@127.0.0.1:14444/Notebook?driver=ODBC+Driver+17+for+SQL+Server&TrustServerCertificate=yes

# 使用者由此連接埠進入網站；例如 5000 就使用 http://伺服器IP:5000/
PUBLIC_HTTP_PORT=5000

# 只供本機 Nginx 與後端使用，不要對外開放。
BACKEND_BASE_PORT=50001
APP_WORKERS=1
WAITRESS_THREADS=8
PDF_TASK_CONCURRENCY=1
```

產生 `SECRET_KEY`：

```powershell
uv run python -c "import secrets; print(secrets.token_hex(32))"
```

密碼若包含 `@`、`:`、`/`、`?`、`#` 等符號，必須先做 URL 編碼。

## 3. 一鍵部署

以 **系統管理員身分** 開啟 PowerShell，進入專案根目錄後執行：

```powershell
Set-Location D:\Notebook-Flask
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\deploy-production.ps1 -NginxExecutablePath C:\nginx\nginx.exe
```

這一個指令會完成：

1. `uv sync` 安裝專案套件。
2. 執行 `deploy\database\script.sql`，在 `DATABASE_URL` 指向的資料庫建立／升級系統資料表與索引。
3. 產生 Nginx 站台設定並檢查語法。
4. 啟動或重新載入 Nginx。
5. 依 `PUBLIC_HTTP_PORT` 建立防火牆規則。
6. 註冊並啟動 `NotebookFlask-01`、`NotebookFlaskTaskWorker`、`NotebookFlaskNginx`。
7. 等待 `/health` 成功；失敗時顯示後端日誌位置。

完成後使用：

```text
http://伺服器IP:PUBLIC_HTTP_PORT/
```

例如 `PUBLIC_HTTP_PORT=5000` 時，網址是 `http://伺服器IP:5000/`。

## 4. 快速檢查與排錯

執行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\check-production.ps1
```

它會顯示後端健康狀態、公開網址與三個開機工作狀態。

若部署失敗，查看後端日誌：

```powershell
Get-Content .\tasks\logs\notebook-flask-50001.log -Tail 100
```

請直接輸入下列網址檢查後端，不要加入方括號或 Markdown 符號：

```powershell
Invoke-RestMethod http://127.0.0.1:50001/health
```

## 5. 解除開機自動啟動

以系統管理員 PowerShell 執行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\unregister-autostart.ps1
```

此動作會移除 Flask、PDF Worker 與 Nginx 的所有 `NotebookFlask*` 開機排程工作。
