# IIS + ARR 一鍵部署

正式架構為 `使用者 → IIS + ARR → Waitress → Flask`。部署腳本會安裝專案套件、建立／升級資料表、建立 IIS 站台與 ARR Server Farm、開放網站連接埠、註冊開機自動啟動，並在後端與公開入口的健康檢查均成功後才顯示完成。

## 1. 第一次前置安裝順序

| 工具 | 用途 | 官方連結 |
| --- | --- | --- |
| Git | 下載與更新專案 | [Git for Windows](https://git-scm.com/download/win) |
| uv | 安裝 Python 套件與建立虛擬環境 | [uv 安裝說明](https://docs.astral.sh/uv/getting-started/installation/) |
| Python 3.12 | 執行系統；也可交由 uv 管理 | [Python for Windows](https://www.python.org/downloads/windows/) |
| ODBC Driver 17 或 18 | Python 連線 SQL Server | [Microsoft ODBC Driver](https://learn.microsoft.com/sql/connect/odbc/download-odbc-driver-for-sql-server) |
| IIS | 對外網站服務 | [安裝 IIS](https://learn.microsoft.com/iis/install/installing-iis-7/installing-iis-on-windows-vista-and-windows-7) |
| IIS URL Rewrite 2.1 | IIS URL 重寫規則 | [URL Rewrite](https://www.iis.net/downloads/microsoft/url-rewrite) |
| IIS Application Request Routing (ARR) | IIS 反向代理與分流 | [ARR](https://www.iis.net/downloads/microsoft/application-request-routing) |

### 1. 安裝 IIS

以系統管理員身分開啟 PowerShell。

Windows Server：

```powershell
Install-WindowsFeature Web-Server, Web-Mgmt-Tools
```

Windows 10／11：

```powershell
Enable-WindowsOptionalFeature -Online -FeatureName IIS-WebServerRole -All -NoRestart
Enable-WindowsOptionalFeature -Online -FeatureName IIS-ManagementConsole -All -NoRestart
```
打開IIS Manager確認是否有安裝成功開啟

### 2. 安裝 URL Rewrite

手動下載並安裝 [IIS URL Rewrite 2.1](https://www.iis.net/downloads/microsoft/url-rewrite)。安裝完成後重新開啟 IIS Manager，應可看到 **URL Rewrite**。

### 3. 安裝 ARR

手動下載並安裝 [IIS Application Request Routing 3.0](https://www.iis.net/downloads/microsoft/application-request-routing)。必須在 URL Rewrite 安裝完成後才安裝 ARR。

安裝完成後重新開啟 IIS Manager；伺服器節點應可看到 **Application Request Routing Cache** 與 **Server Farms**。

### 4. 啟用 ARR 反向代理

可在 IIS Manager 手動啟用：**Application Request Routing Cache** → **Server Proxy Settings** → 勾選 **Enable proxy** → **Apply**。

或直接以系統管理員 PowerShell 執行：

```powershell
Import-Module WebAdministration
Set-WebConfigurationProperty -PSPath "MACHINE/WEBROOT/APPHOST" -Filter "system.webServer/proxy" -Name "enabled" -Value "True"
```

### 5. 驗證前置元件

```powershell
Set-Location D:\Notebook-Flask
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\prepare-iis.ps1
```

成功時會顯示 `IIS, URL Rewrite, and ARR are ready`。

SQL Server 可使用院內既有服務。請建立資料庫 `Notebook` 與網站專用 SQL Login；首次部署帳號需有建立／更新資料表與索引的權限。

## 2. 下載專案與設定 `.env`

```powershell
git clone https://github.com/YuTzu2003/Notebook-Flask.git D:\Notebook-Flask
Set-Location D:\Notebook-Flask
Copy-Item .env.example .env
```

編輯 `D:\Notebook-Flask\.env`：

```ini
APP_ENV=production
APP_DEBUG=false
SECRET_KEY=請填入至少32字元的隨機字串
DATABASE_URL=mssql+pyodbc://notebook_user:密碼@127.0.0.1:14444/Notebook?driver=ODBC+Driver+17+for+SQL+Server&TrustServerCertificate=yes

# 使用者由此連接埠進入網站；例如 5000 就使用 http://伺服器IP:5000/
PUBLIC_HTTP_PORT=5000

# 只供本機 IIS + ARR 與 Waitress 使用，不要對外開放。
BACKEND_BASE_PORT=50001
APP_WORKERS=1
WAITRESS_THREADS=8
PDF_TASK_CONCURRENCY=1
```

產生 `SECRET_KEY`：

```powershell
uv run python -c "import secrets; print(secrets.token_hex(32))"
```

資料庫密碼若包含 `@`、`:`、`/`、`?`、`#` 等符號，必須先做 URL 編碼。

## 3. 一鍵部署

確認 `PUBLIC_HTTP_PORT` 未被其他網站使用後，以 **系統管理員身分** 開啟 PowerShell：

```powershell
Set-Location D:\Notebook-Flask
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\deploy-production.ps1
```

使用 `setup-and-deploy.ps1` 一次驗證前置條件並部署系統：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\setup-and-deploy.ps1
```

指令會：

1. 執行 `uv sync` 與 `deploy\database\script.sql`。
2. 建立 IIS 站台 `NotebookFlask`、`/static/` 虛擬目錄與 ARR Server Farm。
3. 建立 `PUBLIC_HTTP_PORT` 的防火牆規則。
4. 註冊並啟動全部 Waitress 工作程序與 `NotebookFlaskTaskWorker`。
5. 驗證 Waitress 和 IIS + ARR 的 `/health`。

完成後開啟 `http://伺服器IP:PUBLIC_HTTP_PORT/`。

### `HTTP port 80 is already bound by IIS site Default Web Site`

此訊息表示 IIS 的 `Default Web Site` 已綁定 80 埠。建議將 Notebook 改用未占用連接埠，例如編輯 `.env`：

```ini
PUBLIC_HTTP_PORT=5000
BACKEND_BASE_PORT=51001
```

重新執行部署後，以 `http://伺服器IP:5000/` 開啟系統。

若一定要使用 80 埠，先確認 `Default Web Site` 沒有其他系統使用：

```powershell
Import-Module WebAdministration
Get-WebBinding -Name "Default Web Site"
```

確認未使用後，才移除該站台的 80 埠 HTTP binding：

```powershell
Remove-WebBinding -Name "Default Web Site" -Protocol http -Port 80
```

不要只停止 `Default Web Site`，因為 binding 仍會占用 80 埠。移除 binding 後重新執行部署。

## 4. 快速檢查與排錯

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\check-production.ps1
```

此命令會顯示 Waitress、IIS + ARR 健康狀態、IIS 站台狀態與開機工作。若部署失敗，查看：

```powershell
Get-Content .\tasks\logs\notebook-flask-50001.log -Tail 100
Invoke-RestMethod http://127.0.0.1:50001/health
Invoke-RestMethod http://127.0.0.1:5000/health
```

第二個健康檢查連接埠必須改成 `.env` 的 `PUBLIC_HTTP_PORT`。

### `Backend did not become ready` 或 `Cannot overwrite Host`

這表示 Waitress 的開機排程未正常啟動。更新專案內的 `deploy\start-on-boot.ps1` 與 `deploy\register-autostart.ps1` 後，以系統管理員 PowerShell 重新註冊並啟動工作：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\register-autostart.ps1
Start-ScheduledTask -TaskName NotebookFlask-01
Start-ScheduledTask -TaskName NotebookFlaskTaskWorker
```

等待約 30 秒後，執行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\check-production.ps1
```

`pdfrw` 顯示的 `SyntaxWarning: invalid escape sequence` 是第三方套件警告；若日誌後面顯示 `Background task worker started`，Background Worker 已正常啟動。

IIS、ARR 與多人並行請求的完整測試流程請見 [IIS與多人負載測試.md](IIS與多人負載測試.md)。

## 5. 關閉與重新開啟

暫時關閉 Waitress 與 PDF Worker：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\stop-production.ps1
```

重新開啟：

```powershell
Get-ScheduledTask -TaskName "NotebookFlask-??" | ForEach-Object {
    Start-ScheduledTask -TaskName $_.TaskName
}
Start-ScheduledTask -TaskName NotebookFlaskTaskWorker
```

IIS 站台會保留且自行隨 Windows 啟動。若要永久移除 Waitress 與 Worker 的開機排程：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\unregister-autostart.ps1
```
