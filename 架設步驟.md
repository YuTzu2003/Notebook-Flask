# Notebook Flask Windows 架設步驟

本文件適用於 Windows 10／Windows 11 的正式環境。系統使用 SQL Server、Python、Waitress、Nginx 與背景 PDF 工作佇列。
## 1. 安裝 Windows 前置軟體

請下載並安裝下列軟體；安裝程式詢問是否加入 PATH 時，請選擇加入。一般 x64 Windows 請選擇 x64 安裝檔。

| 軟體 | 用途 | 官方下載／安裝說明 |
| --- | --- | --- |
| Python 3.12.x | 執行後端程式 | [Python for Windows](https://www.python.org/downloads/windows/) |
| uv | 建立虛擬環境與安裝 Python 套件 | [uv 安裝說明](https://docs.astral.sh/uv/getting-started/installation/) |
| Git for Windows | 下載及更新專案 | [Git for Windows](https://git-scm.com/download/win) |
| Microsoft ODBC Driver 18 for SQL Server | Python 連線 SQL Server 的必要驅動 | [ODBC Driver 18 下載頁](https://learn.microsoft.com/sql/connect/odbc/download-odbc-driver-for-sql-server) |
| Nginx for Windows | 對外提供網站與靜態檔案 | [Nginx Windows 下載頁](https://nginx.org/en/download.html) |

若要改用命令安裝，可在 PowerShell 使用：

```powershell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
winget install --exact --id nginx.nginx --accept-package-agreements --accept-source-agreements
```

## 2. 下載專案與安裝 Python 套件

在一般 PowerShell 執行：

```powershell
git clone https://github.com/YuTzu2003/Notebook-Flask.git
uv sync
Copy-Item .env.example .env
```

## 3. 設定正式環境 `.env`
```ini
APP_ENV=production
APP_DEBUG=false
SECRET_KEY=請換成隨機64位元以上字串

DATABASE_URL=mssql+pyodbc://notebook_app:請填密碼@127.0.0.1:1433/Notebook?driver=ODBC+Driver+18+for+SQL+Server&TrustServerCertificate=yes

# 2 Core / 16 GB RAM 的建議值
APP_WORKERS=1
PUBLIC_HTTP_PORT=80
BACKEND_BASE_PORT=50001
WAITRESS_HOST=127.0.0.1
WAITRESS_THREADS=8
PDF_TASK_CONCURRENCY=1

SESSION_COOKIE_SECURE=false
ENABLE_SCHEDULER=true
```

產生 `SECRET_KEY`：

```powershell
uv run python -c "import secrets; print(secrets.token_hex(32))"
```

`PUBLIC_HTTP_PORT` 是使用者瀏覽系統的連接埠；`BACKEND_BASE_PORT` 僅供本機 Nginx 與 Waitress 溝通，不要在防火牆對外開放。若日後改動任一連接埠或 `APP_WORKERS`，必須重新執行第 6 與第 8 步。

若已設定 HTTPS，將 `SESSION_COOKIE_SECURE=true`；在純 HTTP 的內網環境維持 `false`，否則瀏覽器不會送出登入 Cookie。

## 5. 建立／升級系統資料表

這個動作會建立缺少的系統資料表與索引，包含使用者、文件、比對、筆記轉移、背景工作佇列與系統日誌；既有資料不會被清除。

```powershell
Set-Location C:\NotebookFlask
uv run python .\deploy\init_database.py
```

成功時會顯示：

```text
Database schema is ready.
```

## 6. 設定 Nginx

先確認 Nginx 的安裝位置。若使用 Winget 預設安裝，請在 Windows 搜尋 `nginx.exe`；以下以 `C:\nginx` 為例。產生專案 Nginx 設定：

```powershell
Set-Location C:\NotebookFlask
.\deploy\configure-nginx.ps1 -ConfigPath C:\nginx\conf\notebook_flask.conf
```

在 `C:\nginx\conf\nginx.conf` 的 `http { ... }` 區塊加入：

```nginx
include notebook_flask.conf;
```

測試並重新載入 Nginx：

```powershell
Set-Location C:\nginx
.\nginx.exe -t
.\nginx.exe -s reload
```

若是首次啟動且尚未有 Nginx 行程，執行 `.\nginx.exe`，再執行 `.\nginx.exe -t` 檢查設定。

## 7. 設定防火牆

以系統管理員 PowerShell 開放使用者存取的 HTTP 連接埠：

```powershell
New-NetFirewallRule -DisplayName "Notebook Flask HTTP" -Direction Inbound -Protocol TCP -LocalPort 80 -Action Allow
```
不要建立 `BACKEND_BASE_PORT`（預設 50001）的對外防火牆規則。若 `PUBLIC_HTTP_PORT` 不使用 80，將上述 HTTP 規則的 `-LocalPort` 改成相同數字。

## 8. 註冊開機自動啟動

以系統管理員 PowerShell 執行：

```powershell
Set-Location C:\NotebookFlask
.\deploy\register-autostart.ps1
Start-ScheduledTask -TaskName NotebookFlask-01
Start-ScheduledTask -TaskName NotebookFlaskTaskWorker
```

系統會建立：

- `NotebookFlask-01`：提供 Web API 的 Waitress 行程。
- `NotebookFlaskTaskWorker`：依序處理 PDF 比對與筆記轉移，避免 2 Core 主機被多個重工作業塞滿。

Nginx 是獨立服務，需依公司既有作法設為開機啟動；首次可手動執行 `C:\nginx\nginx.exe` 驗證。

## 9. 驗證與故障排除

在主機上測試後端：

```powershell
Invoke-RestMethod http://127.0.0.1:50001/health
```

再從同網段使用者電腦開啟：

```text
http://伺服器IP/
```

常用檢查項目：

| 症狀 | 優先檢查 |
| --- | --- |
| 網頁無法開啟 | Nginx 是否執行、Port 80 防火牆、`nginx.exe -t` 是否成功。 |
| 顯示資料庫／ODBC 錯誤 | ODBC Driver 18 是否安裝、`.env` 的 `DATABASE_URL`、SQL Server TCP/IP 與帳密。 |
| 網頁能登入但 PDF 一直排隊 | `NotebookFlaskTaskWorker` 是否執行、`tasks\\logs\\notebook-flask-task-worker.log` 是否有錯誤。 |
| 後端無法啟動 | `tasks\\logs\\notebook-flask-50001.log`、`.env` 是否為 `APP_ENV=production`，以及先執行 `uv sync`。 |

解除自動啟動工作：

```powershell
Set-Location C:\NotebookFlask
.\deploy\unregister-autostart.ps1
```

更新版本與調整 Workers 的說明請見 [系統專案使用.md](系統專案使用.md)。